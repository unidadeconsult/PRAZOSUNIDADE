# No Railway, essa variável é preenchida automaticamente quando você
# adiciona um plugin PostgreSQL e conecta a referência DATABASE_URL a este serviço.
# Para rodar localmente, aponte para um Postgres próprio:
DATABASE_URL=postgres://usuario:senha@localhost:5432/painel_prazos
PORT=3000
